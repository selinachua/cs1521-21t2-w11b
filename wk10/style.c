#define NUM_PROC    573

int func() {
    if (efg) {
        if (jkl) {

        }
    }
}

int main(void) {
    while(TRUE) {
        if (abc) {
            func();
        }
        if (var == 573) {

        }
    }
}

/*
struct person {
    char name[20];
    int age;
    char bday[50];
}
25 structs in the file
one struct = 74 bytes
*/

int func() {
    FILE *fp = fopen(filename, "r");

    while(fseek(fp, 74, SEEK_CURR)) {

    }
}